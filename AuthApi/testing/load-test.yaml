config:
  phases:
    - duration: 120
      arrivalRate: 60

scenarios:
  - flow:
    - post:
        url: "/api/v1/login"
        json: 
          emailAddress: "validuser@test.com"
          password: "password"