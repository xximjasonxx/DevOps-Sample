config:
  phases:
    - duration: 60
      arrivalRate: 7

scenarios:
  - flow:
    - post:
        url: "/api/v1/login"
        json: 
          emailAddress: "validuser@test.com"
          password: "password"