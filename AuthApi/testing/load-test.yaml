config:
  phases:
    - duration: 60
      arrivalRate: 10

scenarios:
  - flow:
    - post:
        url: "/api/v1/login"
        json:
          emailAdress: "validuser@test.com"
          password: "password"